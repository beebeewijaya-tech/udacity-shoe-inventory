<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
  xmlns:app="http://schemas.android.com/apk/res-auto">

  <androidx.constraintlayout.widget.ConstraintLayout
    android:id="@+id/instruction_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingHorizontal="@dimen/medium_padding">

    <ImageView
      android:id="@+id/instruction_illustration"
      android:layout_width="0dp"
      android:layout_height="wrap_content"
      android:layout_marginTop="@dimen/huge_margin"
      android:contentDescription="@string/welcome_illustration"
      android:src="@drawable/ic_instruction"
      app:layout_constraintLeft_toLeftOf="parent"
      app:layout_constraintRight_toRightOf="parent"
      app:layout_constraintTop_toTopOf="parent" />

    <TextView
      android:id="@+id/illustration_title"
      style="@style/title"
      android:layout_marginTop="@dimen/small_margin"
      android:text="@string/intruction_title"
      android:textSize="@dimen/medium_font"
      app:layout_constraintLeft_toLeftOf="parent"
      app:layout_constraintRight_toRightOf="parent"
      app:layout_constraintTop_toBottomOf="@id/instruction_illustration" />

    <TextView
      android:id="@+id/illustration_description"
      style="@style/title"
      app:layout_constraintLeft_toLeftOf="parent"
      app:layout_constraintRight_toRightOf="parent"
      app:layout_constraintTop_toBottomOf="@id/illustration_title"
      android:fontFamily="@font/montserrat"
      android:layout_marginTop="@dimen/tiny_margin"
      android:text="@string/instruction_description"/>

    <Button
      android:id="@+id/button_next"
      style="@style/Button"
      android:layout_marginTop="@dimen/small_margin"
      android:text="@string/i_m_ready_to_start"
      app:layout_constraintLeft_toLeftOf="parent"
      app:layout_constraintRight_toRightOf="parent"
      app:layout_constraintTop_toBottomOf="@+id/illustration_description" />
  </androidx.constraintlayout.widget.ConstraintLayout>
</layout>